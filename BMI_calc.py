# ******* ADDITIONAL MATH FUNCTIONS     ************************
#
# rounding numbers
print(8 / 3) # prints floating number e.g. 2.666666666665
print(int(8 / 3)) # drops everything after decimal and only whole number is presented e.g. 2
print(type(8 / 3)) # determines that this is a floating point number
print(round(8 / 3)) # round to nearest whole number e.g. 3
print(round(8 / 3, 2)) # round to two decimal points e.g. 2.67
print(8 // 3) # Floor Division or round down result e.g. 2.0
result = 4 / 2 # a variable named result that stores the value of the calculation
print result # will print 2.0
result /= 2 # to provide additional calculation on the original result
print(result) # will print 1.0
score = 0 # declaring a score variable
score += 1 # to add 1 to the score variable
score -= 1 # to subtract 1 from the score variable
score /= 1 # to divide score by value of 1
score *= 1 # to multiply score by value of 1
# f-String
score = 0
height = 1.9
isWinning = True
print(f"Your score is {score}, your height is {height}, You are winning is {isWinning})
#
